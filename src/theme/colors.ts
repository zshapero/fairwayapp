/** Visual tokens shared across the home screen, chart, and round detail. */

/** Masters Green — the brand accent. */
export const MASTERS_GREEN = '#0B6B3A';

/** Slightly lighter green used for secondary elements. */
export const MASTERS_GREEN_SOFT = '#147C46';

/** Gold used for the "best ever" annotation and section eyebrows. */
export const ACCENT_GOLD = '#B8862C';

/** Muted gold for tee pills and quieter labels. */
export const ACCENT_GOLD_MUTED = '#9C7327';

/** Muted text color for axis labels and supporting copy. */
export const MUTED_TEXT = '#6B7280';

/** Very low-contrast color for grid lines. */
export const GRID = 'rgba(15, 23, 42, 0.06)';

/** Cream surface used as the page background under the round detail screen. */
export const CREAM = '#F4EFE4';

/** Slightly warmer cream used inside the scorecard card. */
export const CREAM_CARD = '#FAF6EC';

/** Editorial divider — 1px line above and below section headings. */
export const DIVIDER = 'rgba(15, 23, 42, 0.08)';
